# pstack port

- Porting rules and every deliberate departure from upstream, including why `.claude-plugin/plugin.json` has no `version`, live in `.claude/skills/sync-upstream/references/translation.md`.
- Run `bun scripts/check-port.ts` before every commit.
